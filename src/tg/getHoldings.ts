import type { Account } from "viem";

export const getHoldings = async (wallet: Account) => {
  //   const { balance } = await getBalance({
  //     address: wallet.address,
  //     chain: mantleSepoliaTestnet,
  //   });
};
